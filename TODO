Verify that decreasing a register when it's value is 0x00 the result is 0x00 or 0xFF
Verify when to step timer (equal or greater or both)
Implement divider timer (modulo?)
In opcodes when (XY) is used check that we have to address memory[(XY)]...
The issue with flags... actually implement all the cases for the flags